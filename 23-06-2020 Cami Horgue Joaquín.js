osc(40,0.2,2)
  .modulate(
  	noise().pixelate(1000)
  )
  .kaleid(12,4,8)
  .mask(shape(7,0.12).rotate( () => Math.sin(time) ))
  .diff(noise(1000,2,2))
  .repeat(3).diff(osc())
  .scale(10)
  .modulate( osc(102,.1,4,.01))
  .rotate(.1,.2)
  .mask(noise().thresh(0.1).diff( osc(40,0.2,2) ) )
  .color(0.2,1,1,1)
  .diff( voronoi().pixelate() )
  .out()
