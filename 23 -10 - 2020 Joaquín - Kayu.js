voronoi()
  .mult(osc(1,1,1))
  .diff(src(s0))
  .scale(0.8, () => (1.05 + 0.1 * Math.sin(0.05*time)))
  .colorama(() => Math.sin(time % 10 / 10) , () => Math.sin(time % 10 + 3 / 10) ,() => (time % 10) / 100)
  .modulateScale(noise(3))
  .blend(o0, 0.5)
  .modulateScale(o0,0.1)
  .modulate(s0)
  .diff(s0)
  .modulateHue(s0)
  .modulateScale(src(s0).pixelate(100,100))
  .add( shape(4).mask(src(s0)) )
  .out(o0)
